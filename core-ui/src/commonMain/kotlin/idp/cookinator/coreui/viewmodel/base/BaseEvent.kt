package idp.cookinator.coreui.viewmodel.base

/**
 * BaseEvent is a marker interface for events in the MVI architecture. It is used to represent
 * one-time events that occur in the ViewModel, such as navigation actions, showing a toast, or any
 * other side effects that should not be persisted in the state.
 */
interface BaseEvent
