package idp.cookinator.coreui.viewmodel.base

/**
 * BaseIntent is a marker interface for all intents in the MVI architecture. It is used to
 * represent user actions or events that trigger state changes in the ViewModel. Each feature or
 * screen can define its own specific intents by implementing this interface.
 */
interface BaseIntent
